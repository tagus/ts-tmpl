{
  "name": "ts-tmpl",
  "private": true,
  "version": "1.0.0",
  "description": "basic template repo for typescript projects",
  "scripts": {
    "client:start": "pnpm --filter client start:dev",
    "client:build": "pnpm --filter client build:prod",
    "server:start": "pnpm --filter server start:dev"
  }
}
